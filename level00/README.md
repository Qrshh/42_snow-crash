Faire ``` find / -user flag00 2>/dev/null```
Pour trouver les fichiers appartenant a l'utilisateur flag00 (celui sur lequel on cherche a se connecter pour avoir le flag

On tombe sur un code cesar, qu'on s'empresse de decrypter pour tomber sur 
```cdiiddwpgswtgt```
Qui, lorsqu'il est traduit donne : 
```nottoohardhere```
Le mot de passe de connexion est donc nottoohardhere pour l'utilisateur flag00, on se connecte et on recupere notre flag
